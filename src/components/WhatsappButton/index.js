import React from "react"

import * as S from "./styled"

function WhatsappButton() {
  return (
    <S.WrapperButton>
      <S.WrapperIcon>
        <S.Icon />
      </S.WrapperIcon>
      <S.InnerText>Tire suas dúvidas!</S.InnerText>
    </S.WrapperButton>
  )
}

export default WhatsappButton
