import React from "react"

import * as S from "./styled"

export const BtnLight = ({ children }) => (
  <S.WrapperButtonLight>{children}</S.WrapperButtonLight>
)
export const BtnPrimary = ({ children }) => (
  <S.WrapperButton>{children}</S.WrapperButton>
)
